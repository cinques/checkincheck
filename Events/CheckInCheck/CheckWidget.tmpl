<div class="events-CheckWidget">
   <ws:partial
      template="tmpl!Events/BaseCard/Widget"
      switchToCommand="switchToProductList"
      enabled="{{ true }}"
      titleEnabled="{{ true }}"
      title="{[Чеки]}"
   />
</div>